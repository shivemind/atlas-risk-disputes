apiVersion: v1
kind: Service
metadata:
  name: atlas-risk-disputes
  namespace: atlas-production
  labels:
    app: atlas-risk-disputes
spec:
  type: ClusterIP
  selector:
    app: atlas-risk-disputes
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
